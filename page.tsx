import Link from "next/link";
import { createClient } from "@/lib/supabase-server";
export default async function Dashboard(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return <main><div className="container"><h1>Student dashboard</h1><p className="muted">Log in to track your learning.</p><Link className="btn btn-primary" href="/login">Log in</Link></div></main>;const {data:profile}=await supabase.from('profiles').select('full_name').eq('id',user.id).maybeSingle();const {data:progress}=await supabase.from('progress').select('completed,lesson_id');const completed=(progress||[]).filter(p=>p.completed).length;return <main><div className="container"><div className="eyebrow">Student dashboard</div><h1>Welcome{profile?.full_name?`, ${profile.full_name}`:''}.</h1><div className="grid grid-3"><div className="card"><h3>📚 Completed lessons</h3><h2>{completed}</h2><p className="muted">Your completed learning activities.</p></div><div className="card"><h3>🎯 Learning status</h3><p className="muted">Your progress is now connected to Supabase.</p></div><div className="card"><h3>🏆 Certificates</h3><p className="muted">Certificates will appear here when courses are completed.</p></div></div><div className="section card"><h3>Continue learning</h3><p className="muted">Choose a trade and open any lesson.</p><Link className="btn btn-primary" href="/trades">Browse trades</Link></div></div></main>}