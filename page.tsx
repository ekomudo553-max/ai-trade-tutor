'use client'; import {useState} from 'react'; export default function Tutor(){const[q,setQ]=useState('');const[a,setA]=useState('');return <main><div className="container" style={{maxWidth:850}}><div className="eyebrow">AI Trade Tutor</div><h1>Ask a trade question.</h1><p className="muted">Ask for a simple explanation, worked example, practice question or troubleshooting scenario.</p><div className="card"><label>Your question<textarea rows={7} value={q} onChange={e=>setQ(e.target.value)} placeholder="Example: Explain series vs parallel connection in simple terms."/></label><button className="btn btn-primary" onClick={()=>setA('AI Tutor interface is ready. Connect the server route to Vercel AI Gateway for live answers.')}>Ask Tutor</button>{a&&<div className="card" style={{marginTop:18}}><h3>Assistant</h3><p className="muted">{a}</p></div>}</div></div></main>}
