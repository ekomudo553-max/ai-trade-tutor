"use client";
import Link from "next/link";import {useMemo,useState} from "react";
export default function TradeBrowser({trades}:{trades:any[]}){const [q,setQ]=useState('');const [category,setCategory]=useState('All');const categories=[...new Set(trades.map(t=>t.category).filter(Boolean))];const filtered=useMemo(()=>trades.filter(t=>(category==='All'||t.category===category)&&`${t.name} ${t.description}`.toLowerCase().includes(q.toLowerCase())),[trades,q,category]);return <><div className="card" style={{margin:'24px 0'}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search trades, e.g. carpentry, electronics, mechanical..."/><div className="chips">{['All',...categories].map(c=><button type="button" key={c} className={`chip ${category===c?'active':''}`} onClick={()=>setCategory(c)}>{c}</button>)}</div></div><div className="grid grid-3">{filtered.map(t=><Link className="card trade-card" href={`/trades/${t.slug}`} key={t.slug}><div className="trade-icon">{t.icon}</div><span className="badge">{t.category}</span><h3>{t.name}</h3><p className="muted">{t.description}</p><span className="badge">{t.level} · Start →</span></Link>)}</div></> }